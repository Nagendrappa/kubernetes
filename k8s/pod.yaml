apiVersion: v1
kind: Pod
metadata:
  name: webapp-pod
  namespace: dev
  labels:
    app: webapp
spec:
  containers:
    - name: webapp-pod
      image: nagendrappa1/jenkins
      ports:
        - containerPort: 80
        

  







